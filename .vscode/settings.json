{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 200,
            "driver": "SQLite",
            "name": "db",
            "database": "${workspaceFolder}/src/db/db.sqlite"
        },
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "database": "${workspaceFolder}/src/db/db.sqlite.bak",
            "name": "db bak"
        }
    ]
}
